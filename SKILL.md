---
name: album-flower
description: 将专辑封面的配色与创作理念转化为真实花束设计和成品效果图。使用 Codex 内置图像生成工具完成图生图，不要求用户配置 API Key。触发词：专辑花束、专辑封面花束、应援花束、把专辑做成花束、album bouquet。适用于歌迷应援、花艺定制、创意礼物和专辑周边概念设计。
---

# 专辑花束（Album Bouquet）

把一张专辑封面“翻译”成一束真实的高级花束。核心：**检索创作理念 + 提取封面配色与版式 → 真实花材 + 包装还原理念 → 生成杂志级成品图（左/右上角必附封面图）**，花束配色与专辑一致、理念与专辑共鸣。

## 何时使用

用户给出以下任意一种即可触发：

- 一张专辑封面图（最常见）
- 一位艺人 / 一张专辑 / 一首歌的名字
- “专辑花束”“应援花束”“把 XX 专辑做成花束”等表述

## 四步工作流

### 第 0 步：检索创作理念（必做）

用 `web_search` 检索该专辑/歌曲的**创作理念、概念、作者意图、专辑叙事**，关键词如「<专辑名> 概念 创作理念 / concept / 专辑背后的故事」。

提炼 1–3 个可视觉化的理念关键词，例如：

- TWS《SUMMER BEAT!》→ “少年时期的心跳（boyhood pop）”“一起的夏天回忆是进行时（Our Memories: Now）”“友情 / N-S 磁极对立相吸 / 奔跑的青春”

这些理念要**落到花材隐喻上**（写入第 2 步设计稿的“理念呼应”栏），例如：心跳隐喻 → 亮色焦点花；友情/成对 → 成对出现的花材；奔跑/青春 → 向上延伸的线条花材；对立相吸 → 撞色对比。

若无封面图只有名字：同时用 `web_search` 找封面图；找不到则请用户提供封面图。

### 第 1 步：提取配色与版式

分析封面，提炼：

1. **主色**（占比最大 → 花束基底色 + 主包装纸）
2. **辅助色 / 过渡色**（→ 主花材 + 内衬）
3. **点睛色**（高饱和跳色 → 1–2 支焦点花 + 缎带）
4. **可复刻版式元素**：竖排文字、色块分割、logo、签名、颗粒/半调质感等。
   - 标题 / 艺人名 → 复刻到包装纸/背板上，用封面对应字体的彩色。
   - 色块 / 质感 → 用包装纸材质、内衬、背板呼应。

### 第 2 步：设计花束方案

输出结构化设计稿：

- **创作理念**：第 0 步提炼的核心理念（1–2 句）
- **配色提取**：主色 / 辅助色 / 点睛色（附色值）
- **理念呼应**：每个理念关键词如何落到具体花材/造型/包装
- **花材清单**：按「主花 / 焦点花 / 配花 / 叶材 & 线条 / 填充」分层，每项标注「花材名 + 颜色 + 对应封面色块或理念」
- **包装方案**：外层包装纸（色/材质）、内衬、缎带/腰封、可选背板、包材文字（标题/艺人名 + 封面配色）
- **风格定位**：一句话概括气质

约束：
- **花朵必须真实**：只用 `references/flower-palette.md` 收录的现实花/叶/草，不虚构。
- **克制花材（可实际制作）**：每次只选 3–5 种主花/配花 + 1–2 种叶材，疏密有致、留白透气；太密集的花店做不出来，要适度。
- **颜色统一优先用喷漆/染色**：花材颜色不能精准匹配封面时，优先喷漆/染色改色（详见 flower-palette.md），而非堆更多花色。
- **包装材料不限制**：纸/纱/缎带/网纱/铝箔/背板/贴纸任意。
- **配色一致硬性**：花束主色 = 封面主色，点睛色 = 封面跳色。

### 第 3 步：生成高级感成品图（必附封面图）

使用 Codex 的 `imagegen` 技能和内置 `image_gen` 工具完成**图生图（i2i）**。内置模式不需要用户提供或配置 `OPENAI_API_KEY`。

1. 先读取 `imagegen` 技能。仅使用其中的默认内置工具模式，不进入 CLI/API fallback。
2. 本地封面必须先用 `view_image` 打开，使图片进入当前对话上下文；将其明确标记为“参考图 + 左上角封面插图来源 + 配色锚点”。
3. 每张封面单独调用一次内置 `image_gen`。多张封面不得用同一提示批量生成，避免串色和内容混淆。
4. prompt 使用 `Base image / Composition / Lighting / Bouquet structure / Flowers / Packaging / Color / Style` 结构，明确 `Image 1 = album cover`，要求将原封面作为左上角清晰缩略图保留。
5. prompt 必须包含下方“高级感规范”；`Packaging` 同时说明包材如何呼应封面的字体、标题色和版式。对文字准确性没有把握时，保留封面缩略图中的原始文字，包材只使用抽象字形或纯色图形，不伪造乱码标题。
6. 请求竖版约 `1024 × 1536` 的构图。内置工具不接受保存路径时，先生成，再将选定成品复制到用户指定目录；不得覆盖原封面。
7. 用 `view_image` 检查成品的花材真实性、封面缩略图、背景、构图和配色。每张最多针对一个明确问题重试两次。
8. 交付最终文件的绝对路径，并用一句话说明配色与理念如何对应。

若当前会话没有提供内置 `image_gen`，立即说明该能力在本会话不可用并停止生成。不要改用 CLI、第三方生成器或 API，也不要要求用户提供 API Key；建议用户在已启用图像生成能力的 Codex 会话中重新调用本技能。

## 高级感规范（写进每次生成 prompt）

1. **纯黑背景**：哑光纯黑、无反光、无纹理、无渐变、无暗角（不要丝绒/布纹），只做基底。
2. **正放 + 放大**：花束竖直正放（不斜）、居中、整体放大，占画面大部分，只留窄边留白。
3. **封面缩略图**：左上或右上角（默认左上），保持文字与图案清晰可辨。
4. **硬侧光做明暗层次**：方向性硬光在花瓣/纸面打出明暗面，亮部落在花头与包装，阴影干净，背景保持纯黑。
5. **重结构轻饱满**：三角错落立体结构，高低、疏密、质感差（亮面/绒面/蓬松/硬挺）明确，**拒绝圆滚滚花球**。
6. **尖角几何包装**：硬挺卡纸折利落尖角、挺括平整，不用软塌多层褶皱。
7. **色调克制**：2–3 色主导 + 跨元素呼应（封面颜色贯穿花材/包装/背景/缎带/包材文字），精确比例，零杂色。

## 快速规则

- **先检索创作理念**（第 0 步必做），理念落到花材隐喻
- **只用 Codex 内置 `image_gen`**，不使用 CLI/API，不索要 API Key
- 花朵真实（只用 flower-palette.md 收录花材）
- **克制花材，3–5 种主花/配花 + 1–2 种叶材，疏密有致、可实际制作**
- **颜色统一优先喷漆/染色改色（不是堆花色）**
- 配色与专辑一致（硬性）
- 包装材料不限
- **成品图左上/右上必附封面缩略图**（i2i 传入封面图实现）
- **包装纸/背板可复刻专辑标题与艺人名，用封面对应字体的颜色**
- **默认构图：纯黑背景 + 花束正放居中放大 + 封面左上角**
- 效果图默认 1024x1536 竖版
- 同一目标最多生图 2–3 次，未满意则交付最佳版并说明局限
- 花束体上不堆无关文字；封面文字放缩略图与包材（避免 AI 文字错误 + 版权）

## 参考文件

- `references/flower-palette.md` — 真实花材库 + 颜色 → 花材映射
- `references/examples.md` — 已拆解的参考范例（黄黑红、冷蓝系、银黑白等）
- `references/premium-style.md` — 高级感规范详解 + i2i prompt 模板
